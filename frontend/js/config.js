// Local default. On Vercel, replace this value at build/deploy time
// or set window.SHORTLINK_API_BASE before app.js loads.
window.SHORTLINK_API_BASE = window.SHORTLINK_API_BASE || "http://localhost:8080";
