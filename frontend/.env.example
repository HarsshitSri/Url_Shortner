# =============================================================================
# Vercel — Frontend project env (Root Directory: frontend)
# Set these under Project → Settings → Environment Variables (Production).
# Build runs: sh scripts/write-config.sh  (see frontend/vercel.json)
# =============================================================================

# Railway public API URL (no trailing slash)
SHORTLINK_API_BASE=https://REPLACE_ME.up.railway.app

# Optional footer links
SHORTLINK_GITHUB_URL=https://github.com/HarsshitSri
SHORTLINK_LINKEDIN_URL=https://www.linkedin.com/in/harsshit
