/**
 * Auth package placeholder for Version 3.
 *
 * Planned for v3:
 * - JWT register / login
 * - Spring Security filter chain
 * - Persist and enforce short_urls.owner_id
 * - Scope list/update/delete to the authenticated owner
 *
 * Intentionally empty in v2 — no Spring Security dependency yet.
 */
package com.urlshortener.auth;
