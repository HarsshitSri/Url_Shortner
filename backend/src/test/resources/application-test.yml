spring:
  main:
    allow-bean-definition-overriding: true
  ai:
    model:
      chat: none
    google:
      genai:
        api-key: test-key

app:
  base-url: http://localhost:8080
  cors:
    allowed-origins: http://localhost:5500
  jwt:
    secret: test-jwt-secret-key-at-least-32-bytes-long
    expiration-ms: 3600000
